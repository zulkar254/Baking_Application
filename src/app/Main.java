package app;

import web.Server;

public class Main {
    public static void main(String[] args) throws Exception {
        // Start the embedded web server and open browser automatically
        Server.start();
    }
}
